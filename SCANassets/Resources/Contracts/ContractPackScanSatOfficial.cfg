CONTRACT_GROUP
{
    // Name of the contract group
    name = ScanSatOfficial

    minVersion = 1.1.0

    maxSimultaneous = 8
	
	DATA
	{
		type = List<CelestialBody>
		
		p1Bodies = OrbitedBodies().Where(cb => cb.HasSurface())
		p2Bodies = ReachedBodies().Where(cb => cb.HasSurface())
	}
}